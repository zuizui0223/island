name: diagnose_sitelink_recovery_tests

on:
  pull_request:
    branches: [main]
    paths:
      - "src/island_v2/multilingual_wikimedia_sitelink_recovery.py"
      - "tests/test_multilingual_wikimedia_sitelink_recovery.py"
      - ".github/workflows/diagnose-sitelink-recovery-tests.yml"

permissions:
  contents: read

jobs:
  diagnose:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Install package
        run: pip install -e ".[dev]"
      - name: Run sitelink tests and preserve output
        run: |
          set +e
          pytest -vv tests/test_multilingual_wikimedia_sitelink_recovery.py \
            --junitxml=/tmp/sitelink-tests.xml \
            2>&1 | tee /tmp/sitelink-tests.txt
          echo "${PIPESTATUS[0]}" > /tmp/sitelink-tests-exit.txt
          exit 0
      - uses: actions/upload-artifact@v4
        with:
          name: sitelink-recovery-tests-${{ github.run_id }}
          path: |
            /tmp/sitelink-tests.xml
            /tmp/sitelink-tests.txt
            /tmp/sitelink-tests-exit.txt
          if-no-files-found: error
          retention-days: 7
