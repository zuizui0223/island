name: deprecated_global_trait_batch_disabled

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Report deprecation
        run: |
          echo "This workflow is disabled."
          echo "The former per-species extractor required a paid API and has been retired."
          echo "Use the bulk public-trait-source ingestion path instead."
