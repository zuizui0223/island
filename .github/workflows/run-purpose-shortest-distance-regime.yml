name: run_purpose_shortest_distance_regime

on:
  workflow_dispatch:

jobs:
  deferred:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Deferred while PR #126 focuses on direct trait acquisition."
