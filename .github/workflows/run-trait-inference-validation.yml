name: run_trait_inference_validation

on:
  workflow_dispatch:

jobs:
  deferred:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Deferred while PR #126 focuses on direct trait acquisition."
