name: PAUSED -- v2 brms M0-M4 replication

# Paused while the simplified northern Bombus direct/indirect INLA analysis is active.
# Kept as a manual provenance entry only; it does not run on pull requests or pushes.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  paused:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo 'The brms M0-M4 replication workflow is paused.'
          echo 'Use run-v2-inla-category-preserving-north.yml for the active v2 analysis.'
