name: PAUSED -- legacy v2 INLA M0-M4 all-data

# Paused while the simplified northern Bombus direct/indirect analysis is active.
# Kept as a manual provenance entry only; it does not run on pull requests or pushes.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  paused:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo 'This legacy all-data M0-M4 workflow is paused.'
          echo 'Use run-v2-inla-category-preserving-north.yml for the active v2 analysis.'
