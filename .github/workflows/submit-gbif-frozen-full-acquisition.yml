name: paused_gbif_frozen_full_acquisition

# The previous batch run produced accepted keys whose ledger push conflicted.
# Do not submit additional requests until the artifact state is reconciled and
# GBIF's three-active-download limit is checked.
on:
  workflow_dispatch:

jobs:
  paused:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "Submission is paused pending recovery of run 28660644067."
          echo "Use the reconciled capacity-aware workflow, not this retired file."
