name: retired_nonfrozen_gbif_acquisition

# Retained only as a historical pointer. The reproducible workflow is
# submit-gbif-frozen-full-acquisition.yml, which pins the pilot island universe.
on:
  workflow_dispatch:

jobs:
  retired:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "Retired: use submit_gbif_frozen_full_acquisition."
