version: "1.0"
pilot_id: "island-floral-v2-three-block-20260703"
launched_at_utc: "2026-07-03T11:50:00Z"
attempt: 2

# Explicit, bounded launch record: submit exactly three regional GBIF requests.
query_scope:
  acquisition_taxon_name: "Tracheophyta"
  acquisition_taxon_rank: "PHYLUM"
  analytical_target_scope: "angiosperms"
  island_min_area_km2: 5.0
  n_regional_blocks_to_submit: 3

geometry:
  grid_degrees: 30.0
  max_wkt_chars: 120000
  max_islands_per_block: 125
  query_buffer_m: 2000.0
  query_simplify_m: 500.0

post_submit:
  next_step: "read GBIF error bodies; only poll after accepted download keys exist"
  spatial_assignment: "join returned coordinates to original exact island polygons"
  taxonomy_filter: "retain angiosperm records only with provenance retained"
