# Targeted support=2 wave 13 checkpoint

This checkpoint pivots from broad regional crawling to independently sourced third-species evidence for `genus x trait_name` rules that currently have two agreeing direct species.

The two accepted rows are species-direct, fixed-master matches with archived source-byte hashes. `Anthemis rigida` uses individual corolla length rather than capitulum diameter. `Decaspermum fruticosum` uses an explicit botanical description of regular flowers.

Candidates were withheld when the original page could not be archived, the explicit statement applied only at genus scope, or the measured object did not match the target trait. In particular, no search-result snippet was promoted as evidence.
