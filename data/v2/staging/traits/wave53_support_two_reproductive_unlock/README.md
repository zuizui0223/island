# Wave53 support-two reproductive unlock

Wave53 continues the lossless three-axis trait campaign from formal Wave52 without changing the scientific inference contract.

The packet adds exactly two independent species-level reproductive records:

- `Gomphrena serrata` (fixed target): experimental autonomous selfing, promoted conservatively only as `self_incompatibility=SC`.
- `Marcgravia longifolia` (external congener only): paternity-assignment self-fertilization, promoted conservatively only as `self_incompatibility=SC` for trait-specific genus-rule training.

Both target pre-existing `current_min3` support-two rules. The rule remains `genus x axis x trait_name`, requires at least three independent species, uses no family inference and no global fallback, and does not interchange reproductive traits.

The formal workflow regenerates the GBIF exact-match portion of the identity audit, verifies WFO accepted-name anchors, retrieves both cited source pages, checks the frozen source quotes and content fingerprints, rebuilds only the touched rules, and produces a lossless Wave53 overlay.
