# v2 templates

Templates define inputs and review tables; they do not contain biological results.

The GBIF rebuild begins with the island polygon source and produces a raw island-by-species candidate list before this trait template layer is used.
