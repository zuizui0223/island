# Chapter 1 v13 raw-colour result status

PR #233 currently contains three nested analyses:

1. raw five-colour isolation responses;
2. raw colour × raw architecture joint frequencies;
3. raw-colour-conditioned architecture coupling.

The first layer has been reproduced in CI. The latter two are being validated on the same frozen upstream artifacts and must remain secondary until their dedicated runs are green and their result artifacts are inspected.
