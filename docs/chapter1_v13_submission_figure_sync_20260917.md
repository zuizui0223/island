# Chapter 1 v13 submission figure sync — 2026-09-17

Canonical manuscript: `docs/chapter1_manuscript_full_v13_global_pollination_constraint_20260917.md`

Canonical paper lock: `config/chapter1_v13_unified_island_syndrome_result_lock.json`

Canonical functional-bridge lock: `config/chapter1_v13_functional_bridge_result_lock.json`

## Figure 1 — Unified hypothesis and evidence hierarchy

**Purpose:** establish the new paper logic without presenting post-hoc evidence as confirmatory.

Panels:

- **A:** geographic isolation -> pollination-service constraint -> experimental pollen limitation;
- **B:** two partially independent plant routes: reproductive assurance and accessibility/generalization;
- **C:** evidence roles: locked plant associations, locked global GloPL association, post-hoc functional triangulation;
- **D:** taxonomic realization downstream, with broad contemporary and defended native layers separated.

Graphical rule:

- solid arrows = directly supported associations;
- dotted connector from experimental pollen limitation to trait evolution = mechanistic hypothesis consistent with triangulation but not historically identified;
- H4 functional bridge must carry an explicit `post-hoc functional triangulation` label.

Do not draw GloBI or a named pollinator guild as a necessary causal node.

## Figure 2 — Recurrent global island-syndrome direction with regional modification

**Primary source:** `H1_global_recurrent_island_syndrome` in the v13 paper lock.

**A:** six atomic all-analysis standardized distance slopes for the four `analysis_regime` contexts.

**B:** matching Direct-only slopes.

**C:** descriptive mean classic direction by region and evidence scope. Do not add a p-value to the arithmetic mean; significance belongs to the corresponding six-dimensional omnibus response.

**D:** reproductive-assurance and accessibility/generalization descriptive family means, plus the formal North–Tropical joint-difference annotation (`p=0.0007633` all-analysis; `p=0.004502` Direct-only).

Required caption language:

> All four regions show a supported multivariate isolation response and a positive descriptive mean classic direction, while the direct North–Tropical vector test shows that the vectors are not equal. Regional context therefore modifies a recurrent response rather than defining syndrome presence versus absence.

## Figure 3 — Experimental pollen limitation and functional triangulation

**Panel A source:** `H2_global_pollination_constraint`.

Show the full-global GloPL standardized distance coefficient:

- beta `0.0793684`;
- SE `0.0377342`;
- two-sided `p=0.0354348`;
- one-sided positive `p=0.0177174`;
- support: 2,969 effect rows / 1,248 sites / 919 publications.

**Panel B:** post-hoc GloPL shape diagnostic, visually separated from the parent confirmatory result:

- within-offshore beta `0.199830`, p=`0.01110`;
- offshore-only beta `0.235453`, p=`0.005865`.

**Panel C source:** `config/chapter1_v13_functional_bridge_result_lock.json`.

Forest plot of the four evaluable primary trait-level estimates:

- autonomous selfing `-0.446724`;
- actinomorphy `-0.381194`;
- generalized form `-0.184104`;
- self-compatibility `-0.117727`.

Visually distinguish strong/reproduced, global-only, primary-only and imprecise classifications rather than applying one binary significance label.

**Panel D:** autonomous-selfing robustness only:

- primary `-0.446724`;
- supplemental-only `-0.303724`;
- no-zero-constant `-0.454798`;
- within publication `-0.306034`;
- within publication×site `-0.186358`.

Also show a small inset or annotation that the frozen Route A/B distance-by-trait moderation families remained `0/3` promoted; v13 does not reclassify them.

## Figure 4 — Taxonomic realization and floristic-status boundary

**Panel A:** broad all-observed H3A.

- common-support H2 gate: all `p=0.00008393`, Direct `p=0.00012109`;
- after source-free genus residualization: all `p=0.01150`, Direct `p=0.004593`.

Interpretation: broad contemporary context response is not erased by source-free genus composition.

**Panel B:** defended native Palearctic H3B.

- stage support `4/4 -> 4/4 -> 0/4`;
- matched pseudo-genus `p=0.0289855`.

Interpretation: source-backed native Palearctic response is strongly genus-structured.

**Panel C:** same-375-island floristic-status boundary.

- all observed: `p=0.0005607` / Direct `0.0003959`;
- source-backed native: `p=0.2491` / Direct `0.3018`.

Optionally annotate WCVP regional-native-compatible expansion: 2,372 islands, `p=0.07954` / Direct `0.17472`.

**Panel D:** claim boundary diagram.

Allowed: taxonomic realization differs among flora layers.

Blocked: all-observed = native assembly; genus attenuation = causal mediation; genus attenuation proves plant dispersal or pollinator filtering.

## Supplementary placement

The following do not belong in the main causal figure sequence:

- GloBI source-breadth heterogeneity (`3/4` source modes): supplementary interaction-record sensitivity;
- named large-bee/butterfly/bird template results: supplementary floral-architecture concordance;
- exact-island GloPL null/weak analysis: support-limited sensitivity to the full-global GloPL frame;
- P2/P3 legacy defense surfaces: retain as supplementary provenance supporting regional and observation-process boundaries;
- area moderation and nonlinear-geometry audits: retain as falsification/robustness supplements.

## Evidence-label rule

Every figure panel must use one of these labels:

- `frozen primary / confirmatory parent result`;
- `frozen sensitivity / robustness`;
- `post-hoc functional triangulation`;
- `descriptive synthesis`.

No figure may visually upgrade a post-hoc or descriptive result by using the same inferential label as the frozen parent result.
