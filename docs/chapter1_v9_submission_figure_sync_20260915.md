> **HISTORICAL / SUPERSEDED — pre-corrected Chapter 1 surface.** Retained for provenance/replay only. The current submission is selected by `config/chapter1_submission_current.json`; use `submission/chapter1_current/MANUSCRIPT.md` and `docs/PAPER_PIPELINE.md` for current results.

# Chapter 1 v9 submission figure sync — 2026-09-15

Status: current manuscript-to-figure contract after P1 defense.

The v9 manuscript is island-first and uses the P1a/P1c/P1d result locks to narrow H3 without reopening the historical H3 gate.

## Figure 1 — island question to taxonomic assembly

Canonical lock remains:

- `config/chapter1_v8_figure1_result_lock.json`

Role in v9:

1. begin with the island-flower question;
2. distinguish repeated within-lineage response from differential lineage assembly;
3. show biogeographic branching and taxonomic composition as sequential questions;
4. hand unresolved local mechanism/geometry to Chapter 2.

The legend must not describe the family-to-genus increment as precisely estimated. Figure 1 remains conceptual; P1 precision is shown in Figure 3.

## Figure 2 — context-dependent joint response

Canonical lock remains:

- `config/chapter1_v8_figure2_result_lock.json`

Role unchanged: primary H1/H2 response vectors, evidence-scope/stratum replication, and conditional selfing-core decomposition.

## Figure 3 — P1-defended genus structure

New canonical lock:

- `config/chapter1_v9_figure3_p1_defense_result_lock.json`

Workflow:

- run `34942779753`;
- artifact `10386555245`;
- digest `sha256:93957ed4e12193fd2630aa3c274a99f004af4c8e40a6f63985e46a77c4cdc239`.

Panel mapping:

- **A — Frozen taxonomic attenuation profiles.** Retains the historical observed → family → genus trajectories and descriptive attenuation ranges.
- **B — True genus versus matched-complexity pseudo-genus null.** 2,000 valid permutations; true-genus conditional attenuation `0.72066`, null median `0.22434`, 57/2,000 null values ≥ observed, one-sided `p=0.02899`.
- **C — Paired spatial-block uncertainty.** Eight direct-only primary profiles; all eight 95% intervals for the *additional* family-to-genus attenuation include zero.
- **D — Integrated claim boundary.** Same observations/weights: PASS; true genus > arbitrary matched grouping: PASS; large total genus attenuation: PASS; exact incremental family→genus attenuation precisely estimated: NO; causal assembly mechanism identified: NO.

Final Figure 3 legend:

> **Figure 3 | The Palearctic floral-island response is genus-structured beyond matched grouping complexity, but the exact taxonomic increment is imprecise.** Frozen taxonomic-depth trajectories show strong attenuation after source-matched genus adjustment across the Palearctic profiles (A). A matched-complexity randomization compares true genera with pseudo-genera formed within family while preserving the exact genus-count and genus-size multiset: true genus membership produces a median conditional attenuation of `0.721`, compared with a pseudo-genus null median of `0.224`; 57/2,000 null permutations equal or exceed the true-genus statistic (`p=0.02899`; B). Paired spatial-block bootstrap intervals provide the counterweight: although total genus attenuation remains large, all eight direct-only 95% intervals for the *additional* family-to-genus attenuation include zero (C). The integrated decision is therefore genus-specific taxonomic structure without a precisely estimated family-to-genus breakpoint or a causal assembly mechanism (D).

## Figure 4 — claim boundaries beyond P1

Canonical lock remains:

- `config/chapter1_v8_figure4_result_lock.json`

Role unchanged:

- V6 species-detection tipping;
- calibrated observed response geometry;
- H5c independent biotic-vs-wind specificity;
- H5d distributed-threshold non-identifiability.

## Main-text figure order

The submission narrative is fixed as:

1. **Fig. 1:** why the island-flower problem requires an assembly-level test;
2. **Fig. 2:** the expected syndrome is not globally uniform and its components can decouple;
3. **Fig. 3:** the strongest Palearctic branch is specifically genus-structured, but the exact family→genus increment is uncertain;
4. **Fig. 4:** observation, geometry and mechanism cross-examination defines the remaining claim ceiling.

## Claim ceiling synchronized to figures

Allowed:

- one universal floral island syndrome is not recovered;
- Palearctic and tropical response vectors differ in composition/direction, with tropical accessibility less observation-robust;
- the Palearctic response is strongly structured by source-matched genus composition;
- true genus boundaries outperform arbitrary within-family partitions of identical grouping complexity (`p=0.02899`);
- several simple alternative explanations fail explicit gates.

Required caveat:

- the additional family-to-genus attenuation is spatially imprecise: 0/8 direct-only paired bootstrap intervals exclude zero.

Not allowed:

- a precisely estimated taxonomic breakpoint at family→genus;
- genus as a causal mechanism;
- dispersal-only assembly;
- absence of within-lineage evolution;
- pollinator loss as the identified generator;
- a universal local threshold inferred from global distance.
