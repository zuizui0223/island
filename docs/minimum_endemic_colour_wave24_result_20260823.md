# Minimum endemic-colour wave24 formal result — 2026-08-23

## Formal evidence result

PR #132 run `32637872865` rebuilt the canonical all-evidence ledger from the latest prior formal artifact (`32551742699`) plus exactly three individually reviewed species-direct flower-colour records:

- `Astragalus kawakamii` -> `yellow_orange`;
- `Limonium aphroditae` -> `blue_purple`;
- `Viola cephalonica` -> `blue_purple`.

The promotion validator accepted all 3/3 rows. Formal rebuild result:

- new direct species-trait rows: **3**;
- new direct species-axis cells: **3**;
- flower-colour strict coverage: **42,256 -> 42,259**;
- total strict species-axis coverage: **160,946 -> 160,949**;
- gross gain: **3**, loss: **0**;
- Validated-Low added: **0**;
- Validated-Low invalidated: **0**;
- Validated-Low value changes: **0**.

Thus the gain is direct evidence, not genus-inference amplification.

## Endemic-colour model-support implication

All three species belong to distinct northern-midlatitude endemic-colour target islands in the frozen minimum queue from run `32614335237`.

Therefore northern endemic-colour direct support moves exactly:

**31 -> 34 islands**

and the remaining gap to the 50-island continuation target is:

**16 islands**.

Two of the three completed queue rows also extend the previous direct-supported isolation 5-95% envelope:

- `Limonium aphroditae` (original selection order 10);
- `Astragalus kawakamii` (order 11).

`Viola cephalonica` (order 19) adds a distinct island inside the previous envelope.

Tropical endemic-colour support remains **46 / 50**. The current four-species tropical queue has not yielded an ontology-safe direct plain/non-plain value; e.g. the official `Dracaena fernaldii` description gives greenish-yellow petals, which crosses the current plain/non-plain boundary.

## Remaining frozen queue

Do not regenerate or widen the target universe merely because three rows were completed. The original 23-species queue remains the provenance object; operational continuation excludes the three completed northern species.

Remaining northern queue: **16 species / maximum 16 new islands**.
Remaining tropical queue: **4 species / maximum 4 new islands**.

The next accepted northern record therefore changes support from 34 to 35; acquisition stops at 50. Tropical acquisition stops immediately after four additional supported islands, if all four can be resolved under the unchanged evidence contract.

## Interpretation boundary

This result improves precision for the endemic-colour stratum only. It does not alter the earlier falsification results:

- broad plain/generalized/SC isolation shifts do not survive floristic-status + genus control;
- the Bombus environmental hypervolume does not recover the predicted northern mechanism after those controls;
- additional colour acquisition must not be used to rescue either rejected broad claim.
